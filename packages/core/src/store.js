/* eslint-disable */
export default class Store {
    constructor() {
    }

    get(key) {

    }

    set(key, value) {
    }

    size() {
        return 0;
    }

    isEmpty(value) {
        return (value === undefined || value === null);
    }

    pipe(stream) {
    }
}
