[use]
plugin = basics
plugin = analytics

[JSONParse]
separator = *

[env]
path = manifest.identifier
value = get('value')

[exchange]
value = get('value')

[FILELoad]
location = /tmp

[unpack]

[TARDump]
compress = true
manifest = env('manifest')
manifest = fix({description: 'Exemple de fichier batch pour traitement asynchrone'})
