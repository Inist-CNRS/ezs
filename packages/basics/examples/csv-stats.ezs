#!/usr/bin/env ezs

# Take CSV input and get JSON

[use]
plugin = basics

# Parse CSV input
[CSVParse]

# Transform each line on a object
[CSVObject]


[substitute]
key = countColumns
value = keys().size()
key = size
value = mapValues(_.size).values().sum()


# generate JSON
[JSONString]
indent = true

